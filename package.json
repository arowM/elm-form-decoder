{
  "private": true,
  "scripts": {
    "format": "elm-format src",
    "start": "elm-doc-preview",
    "test": "elm-analyse && elm-format --validate src && elm make --docs=/tmp/docs.json && elm-verify-examples"
  },
  "devDependencies": {
    "elm-analyse": "^0.16.5",
    "elm-doc-preview": "^2.0.5",
    "elm-format": "^0.8.2",
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^3.0.1"
  },
  "dependencies": {
    "elm": "^0.19.1-3"
  }
}
